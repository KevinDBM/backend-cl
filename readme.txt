Bimbau's Boilerplate

replace all SERVICEPORT by the port of the service, remember checking the current services to accumulate it in order.
replace all BOILERPLATE for the name of the service, and check the name that end with service.